export default defineEventHandler((event) => {
  // I can put some props into link
  // const {props} = useQuery(event)
  // const {props} = useBody(event)
  // const {test} = event.context.params
  // const { testKey } = useRuntimeConfig(); env value
});
